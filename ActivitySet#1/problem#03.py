# Variables, Expressions & Statements

hrs = float(input("Enter hours? "))
rate = input('enter the rate per hour:')
payment = float(hrs) * float(rate)
print('payment to made for the respective hrs is:',payment)

